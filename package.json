{
  "name": "danix-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Danix AI-Powered Autonomous Vulnerability Assessment & Penetration Testing Platform",
  "scripts": {
    "dev": "concurrently -k -p \"[{name}]\" -c \"cyan.bold,magenta.bold\" -n \"backend,frontend\" \"npm run backend\" \"npm run frontend\"",
    "frontend": "npm --prefix frontend run dev",
    "backend": "cd pentest_backend && (.\\.venv\\Scripts\\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload || python -m uvicorn app.main:app --host 127.0.0.1 --port 8000 --reload)"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
